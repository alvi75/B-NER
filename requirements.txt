# B-NER runtime dependencies.
#
# Only the packages the notebooks actually import are listed here. The previous
# version of this file was a full `pip freeze` of a 2021 conda environment: it
# pinned ~120 packages, most of them transitive or Jupyter tooling, which is why
# the repo accumulated hundreds of Dependabot alerts. The historical pins are
# still in git history if an exact 2021 replay is ever needed.
#
# Lower bounds are the minimum versions with no known open advisory.

# --- modelling ---
transformers>=5.10.0
torch>=2.13.0
tokenizers
datasets
evaluate
seqeval

# --- data / analysis ---
numpy>=1.26
pandas>=2.0
scikit-learn>=1.5.0
matplotlib>=3.8
openpyxl>=3.1

# Jupyter is deliberately NOT pinned here. Install it separately so its large
# transitive tree is not part of this project's dependency graph:
#     pip install jupyterlab

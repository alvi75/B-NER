# B-NER runtime dependencies.
#
# Only what this project needs at RUNTIME is listed here. That is not the same
# as what the notebooks import: seqeval, openpyxl and accelerate (via the
# transformers[torch] extra) are required but never appear in an import line.
#
# The previous version of this file was a full `pip freeze` of a 2021 conda
# environment: 127 pinned packages, most of them transitive or Jupyter tooling,
# which is why the repo accumulated 235 Dependabot alerts. The historical pins
# are still in git history if an exact 2021 replay is ever needed.
#
# Where a lower bound is given it is the oldest version with no known open
# advisory. Entries with no bound have no advisories on record at all.
#
# Validated on Python 3.13. Python 3.10/3.11 install fine but resolve older
# numpy/pandas/scikit-learn than were tested - prefer 3.12+.

# --- modelling ---
transformers[torch]>=5.10.0
torch>=2.13.0
tokenizers
datasets
evaluate
seqeval

# --- data / analysis ---
numpy>=1.26
pandas>=2.0
scikit-learn>=1.5.0
matplotlib>=3.8
openpyxl>=3.1

# Jupyter is deliberately NOT pinned here. Install it separately so its large
# transitive tree is not part of this project's dependency graph:
#     pip install jupyterlab
